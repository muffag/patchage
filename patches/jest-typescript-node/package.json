{
  "scripts": {
    "test": "jest",
    "test:watch": "jest --watch",
    "test:cov": "jest --coverage && open coverage/lcov-report/index.html"
  },
  "devDependencies": {
    "jest": "^24.9.0",
    "ts-jest": "^24.2.0",
    "@types/jest": "^24.0.23"
  },
  "jest": {
    "preset": "ts-jest",
    "collectCoverageFrom": [
      "src/**/*.ts"
    ]
  }
}
